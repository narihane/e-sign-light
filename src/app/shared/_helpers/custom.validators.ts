import { FormArray, FormControl, FormGroup, ValidationErrors } from '@angular/forms';

export class CustomValidators {
  // static clientID(c: FormControl, totalPrice:Number): ValidationErrors {
  //   const num=Number(c.value);
  //   const isValid=!isNaN(num) && c.value ==='' && totalPrice >= 50000;
  //   const message = ''

  //   return isValid ? null//: new;
  // }
}
// f (control.value && control.get("fixed")?.get("client_id")?.value === ''
// && parseInt(control.get("fixed")?.get("totalPrice")?.value) >= 50000) {
// return { 'clientIDInvalid': true };
// }
